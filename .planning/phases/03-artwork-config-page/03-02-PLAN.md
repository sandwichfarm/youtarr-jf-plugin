---
phase: 03-artwork-config-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Jellyfin.Plugin.Youtarr/Configuration/configPage.html
  - Jellyfin.Plugin.Youtarr/Configuration/PluginConfiguration.cs
autonomous: true
requirements: [PLUG-03]
must_haves:
  truths:
    - "The Dashboard config page renders a year-seasons checkbox, an episode-numbering select (Default / YYYYMMDD), and a max-description-length number input"
    - "On page show the three controls are populated from the saved PluginConfiguration via ApiClient.getPluginConfiguration"
    - "On save the three values are written back via ApiClient.updatePluginConfiguration using the plugin GUID 80302d7f-7fc3-4b1c-9a3f-fd85b98b9a69"
    - "PluginConfiguration exposes YearSeasons, EpisodeNumberingScheme, and MaxDescriptionLength so the page binds to real fields"
  artifacts:
    - path: "Jellyfin.Plugin.Youtarr/Configuration/configPage.html"
      provides: "Three working controls wired to ApiClient get/update plugin configuration"
      contains: "updatePluginConfiguration"
    - path: "Jellyfin.Plugin.Youtarr/Configuration/PluginConfiguration.cs"
      provides: "YearSeasons + EpisodeNumberingScheme + MaxDescriptionLength fields with defaults"
      contains: "MaxDescriptionLength"
  key_links:
    - from: "Jellyfin.Plugin.Youtarr/Configuration/configPage.html"
      to: "ApiClient plugin configuration endpoints"
      via: "getPluginConfiguration / updatePluginConfiguration with the plugin GUID"
      pattern: "80302d7f-7fc3-4b1c-9a3f-fd85b98b9a69"
    - from: "Jellyfin.Plugin.Youtarr/Configuration/configPage.html"
      to: "Jellyfin.Plugin.Youtarr/Configuration/PluginConfiguration.cs"
      via: "config property names matching C# property names exactly"
      pattern: "EpisodeNumberingScheme"
---

<objective>
Deliver PLUG-03: replace the Phase 1 stub `configPage.html` with three working Dashboard controls — a year-seasons checkbox (`YearSeasons`), an episode-numbering `<select>` (`EpisodeNumberingScheme`: Default / YYYYMMDD), and a max-description-length number input (`MaxDescriptionLength`) — wired via `ApiClient.getPluginConfiguration` / `updatePluginConfiguration` against the permanent plugin GUID. Ensure `PluginConfiguration` exposes all three fields with sensible defaults so the page binds to real, persisted settings.

Purpose: Phase 2 plans (02-02) add `EpisodeNumberingScheme` and `MaxDescriptionLength` to `PluginConfiguration`, but Phase 2 has not been executed yet (no Phase 2 SUMMARYs on disk; PluginConfiguration currently has only `YearSeasons`). This plan is **self-sufficient**: it idempotently ensures the two fields exist (adding them with documented defaults only if absent) so the config page is fully functional and unit-buildable independent of Phase 2. If Phase 2 later runs, the field definitions are already in place and compatible.
Output: A fully fleshed-out `configPage.html` matching the verified tubearchivist-jf-plugin pattern, and a `PluginConfiguration.cs` exposing the three bound fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-artwork-config-page/03-RESEARCH.md
@Jellyfin.Plugin.Youtarr/Configuration/PluginConfiguration.cs
@Jellyfin.Plugin.Youtarr/Configuration/configPage.html
@Jellyfin.Plugin.Youtarr/Plugin.cs

<interfaces>
<!-- Plugin GUID (Plugin.cs StaticId) — MUST match exactly, never change: 80302d7f-7fc3-4b1c-9a3f-fd85b98b9a69 -->
<!-- configPage.html is already an EmbeddedResource (csproj line: <EmbeddedResource Include="Configuration/configPage.html" />) -->
<!-- Plugin.GetPages() already serves it as EmbeddedResourcePath = Namespace + ".Configuration.configPage.html" — NO change to Plugin.cs needed -->
<!-- BasePlugin<PluginConfiguration> auto-serializes config to config/plugins/<GUID>/<GUID>.xml; no persistence code needed -->
<!-- Config property names sent by JS MUST match C# property names case-sensitively (YearSeasons, EpisodeNumberingScheme, MaxDescriptionLength) -->
<!-- EpisodeNumberingScheme serializes by enum NAME in XML — <select> option values MUST be "Default" / "YYYYMMDD" exactly (RESEARCH Pitfall 4) -->
<!-- MaxDescriptionLength is int — JS MUST parseInt(value, 10) before assigning (RESEARCH Pitfall 5) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure PluginConfiguration exposes the three bound fields</name>
  <read_first>
    - Jellyfin.Plugin.Youtarr/Configuration/PluginConfiguration.cs (current state — only YearSeasons exists)
    - .planning/phases/03-artwork-config-page/03-RESEARCH.md (Config defaults + new fields row; Pitfall 4 enum-name serialization)
    - .planning/ROADMAP.md (Phase 2: EPI-04 default 500 chars, EPI-05 Default vs YYYYMMDD scheme — the canonical semantics for these fields)
  </read_first>
  <action>Open PluginConfiguration.cs. Keep YearSeasons (bool, default true) unchanged. If EpisodeNumberingScheme is not already present, add a public enum EpisodeNumberingScheme with exactly two members named Default and YYYYMMDD (in that declaration order, Default first) in the Jellyfin.Plugin.Youtarr.Configuration namespace, and a public EpisodeNumberingScheme EpisodeNumberingScheme property defaulting to EpisodeNumberingScheme.Default. If MaxDescriptionLength is not already present, add a public int MaxDescriptionLength property defaulting to 500 (per EPI-04). Use the enum member names Default and YYYYMMDD verbatim — they are the literal string values the config page select binds to via XML name-serialization. Add XML doc comments matching the existing YearSeasons style. This task is idempotent: if Phase 2 already added either field, leave it as-is and do not duplicate. Do NOT add server-side clamping logic here (the provider that consumes MaxDescriptionLength lives in Phase 2); only the field + default belong in this plan.</action>
  <verify>
    <automated>dotnet build Jellyfin.Plugin.Youtarr/Jellyfin.Plugin.Youtarr.csproj -c Release 2>&1 | grep -qiE "Build succeeded|0 Error" && grep -q "MaxDescriptionLength" Jellyfin.Plugin.Youtarr/Configuration/PluginConfiguration.cs && grep -q "EpisodeNumberingScheme" Jellyfin.Plugin.Youtarr/Configuration/PluginConfiguration.cs && grep -q "YYYYMMDD" Jellyfin.Plugin.Youtarr/Configuration/PluginConfiguration.cs && echo OK</automated>
  </verify>
  <done>PluginConfiguration.cs compiles in Release and exposes YearSeasons (default true), EpisodeNumberingScheme (enum Default/YYYYMMDD, default Default), and MaxDescriptionLength (int, default 500); no duplicate definitions if a field pre-existed.</done>
</task>

<task type="auto">
  <name>Task 2: Flesh out configPage.html with three controls wired to ApiClient</name>
  <read_first>
    - Jellyfin.Plugin.Youtarr/Configuration/configPage.html (current stub to replace)
    - .planning/phases/03-artwork-config-page/03-RESEARCH.md (full configPage.html pattern; Pitfalls 4 and 5; "Key details from tubearchivist-jf-plugin pattern")
    - Jellyfin.Plugin.Youtarr/Plugin.cs (confirm StaticId GUID value for the pluginUniqueId constant)
  </read_first>
  <action>Replace the body of configPage.html with the three-control form from the RESEARCH pattern. Keep the page div id YoutarrConfigPage, data-role="page", class "page type-interior pluginConfigurationPage", and update data-require to "emby-input,emby-button,emby-select,emby-checkbox" (add emby-select since the stub omits it). Add a form with id YoutarrConfigForm containing: (a) an emby-checkbox input id/name YearSeasons with a description; (b) an emby-select id/name EpisodeNumberingScheme with exactly two options whose value attributes are "Default" and "YYYYMMDD" verbatim (matching the C# enum member names — RESEARCH Pitfall 4); (c) an emby-input type=number id/name MaxDescriptionLength with min="0" and a description noting default 500 and 0 = no truncation; (d) an emby-button type=submit "Save". Add the script block: define pluginUniqueId = '80302d7f-7fc3-4b1c-9a3f-fd85b98b9a69' (must match Plugin.cs StaticId exactly). On the page's pageshow event call Dashboard.showLoadingMsg(), then ApiClient.getPluginConfiguration(guid) and populate the three controls from config.YearSeasons (checkbox .checked), config.EpisodeNumberingScheme (select .value), config.MaxDescriptionLength (number .value), then Dashboard.hideLoadingMsg(). On the form submit event call Dashboard.showLoadingMsg(), re-fetch config via getPluginConfiguration, assign config.YearSeasons from the checkbox .checked, config.EpisodeNumberingScheme from the select .value, config.MaxDescriptionLength from parseInt(input.value, 10) (RESEARCH Pitfall 5 — int field), then ApiClient.updatePluginConfiguration(guid, config) and on resolve call Dashboard.processPluginConfigurationUpdateResult(result); preventDefault and return false. Property names in JS MUST match the C# property names case-sensitively. Do not render any user-supplied string into the DOM (XSS mitigation — see threat model).</action>
  <verify>
    <automated>grep -q "80302d7f-7fc3-4b1c-9a3f-fd85b98b9a69" Jellyfin.Plugin.Youtarr/Configuration/configPage.html && grep -q "updatePluginConfiguration" Jellyfin.Plugin.Youtarr/Configuration/configPage.html && grep -q 'value="YYYYMMDD"' Jellyfin.Plugin.Youtarr/Configuration/configPage.html && grep -q "parseInt" Jellyfin.Plugin.Youtarr/Configuration/configPage.html && grep -q "emby-select" Jellyfin.Plugin.Youtarr/Configuration/configPage.html && dotnet build Jellyfin.Plugin.Youtarr/Jellyfin.Plugin.Youtarr.csproj -c Release 2>&1 | grep -qiE "Build succeeded|0 Error" && echo OK</automated>
  </verify>
  <done>configPage.html contains the three controls (YearSeasons checkbox, EpisodeNumberingScheme select with "Default"/"YYYYMMDD" options, MaxDescriptionLength number input), the correct plugin GUID, getPluginConfiguration on pageshow, updatePluginConfiguration on submit, parseInt for the number field, and emby-select in data-require; the embedded-resource build succeeds. Live persistence is confirmed in 03-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard browser → plugin config API | Admin-only (Jellyfin restricts the config page to admin users); config values cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | configPage.html MaxDescriptionLength input | mitigate | `parseInt(value, 10)` client-side so the server receives an int, not a string (RESEARCH Pitfall 5); server-side clamp to >=0 lives in the Phase 2 consumer, not this page |
| T-03-02 | Tampering (XSS) | configPage.html DOM | mitigate | Page renders no user-supplied strings into the DOM; Jellyfin Dashboard handles HTML encoding of any framework-rendered values |
| T-03-03 | Elevation of Privilege | config page access | accept | Jellyfin Dashboard restricts plugin config pages to admin users automatically; plugin adds no auth surface of its own (ASVS V4 partial — handled by host) |
</threat_model>

<verification>
- Release build succeeds with the updated embedded resource.
- No regression in the existing test suite: `dotnet test Jellyfin.Plugin.Youtarr.Tests/Jellyfin.Plugin.Youtarr.Tests.csproj -c Release`.
- `grep` confirms the GUID, updatePluginConfiguration, "YYYYMMDD" option value, parseInt, and emby-select are all present in configPage.html.
- Live render + persistence-across-restart is verified in the 03-03 checkpoint (requires Docker harness).
</verification>

<success_criteria>
- PLUG-03 code-complete: config page shows the three controls and is wired to ApiClient get/update with the correct GUID.
- PluginConfiguration exposes all three bound fields with documented defaults (YearSeasons true, EpisodeNumberingScheme Default, MaxDescriptionLength 500).
- Self-sufficient: builds and binds correctly whether or not Phase 2 has run.
</success_criteria>

<output>
Create `.planning/phases/03-artwork-config-page/03-02-SUMMARY.md` when done.
</output>
