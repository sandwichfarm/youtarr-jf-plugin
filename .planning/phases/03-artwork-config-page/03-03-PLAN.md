---
phase: 03-artwork-config-page
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - test/jellyfin-load-test/media/MyChannel/poster.jpg
  - test/jellyfin-load-test/scripts/make-image-fixtures.sh
  - test/jellyfin-load-test/README.md
autonomous: false
requirements: [ART-01, ART-02, ART-03, ART-04, PLUG-03]
must_haves:
  truths:
    - "The channel poster.jpg displays as the Series Primary image in the library grid (ART-01, built-in)"
    - "The channel poster.jpg is also shown as the Series Backdrop on the Series detail page (ART-02, custom provider)"
    - "A per-video thumbnail displays as the Episode Primary image when Phase 2 episodes exist (ART-03, built-in)"
    - "A channel with no poster.jpg and an episode with no thumbnail both display without errors and fall back to no image (ART-04)"
    - "The Dashboard config page renders all three controls and a saved MaxDescriptionLength change persists across a Jellyfin restart (PLUG-03)"
  artifacts:
    - path: "test/jellyfin-load-test/media/MyChannel/poster.jpg"
      provides: "Valid small JPEG channel-art fixture for ART-01/ART-02 live verification"
    - path: "test/jellyfin-load-test/scripts/make-image-fixtures.sh"
      provides: "Reproducible script that generates the minimal JPEG fixtures (committed for repeatability)"
    - path: "test/jellyfin-load-test/README.md"
      provides: "Operator instructions for the artwork + config live verification, including the sudo docker command"
  key_links:
    - from: "test/jellyfin-load-test/media/MyChannel/poster.jpg"
      to: "Series MyChannel ImageTags.Primary + BackdropImageTags"
      via: "Jellyfin image refresh picking up poster.jpg (built-in Primary, custom Backdrop)"
      pattern: "poster.jpg"
    - from: "Jellyfin.Plugin.Youtarr/Configuration/configPage.html"
      to: "config/plugins/<GUID>/<GUID>.xml"
      via: "ApiClient.updatePluginConfiguration persisting across restart"
      pattern: "MaxDescriptionLength"
---

<objective>
Prove the Phase 3 deliverables work in a real Jellyfin 10.10.7 instance: add image fixtures to the Docker load-test harness, deploy the freshly built plugin DLL, and run a human-verify checkpoint that confirms ART-01 (Series Primary from built-in), ART-02 (Series Backdrop from the custom provider), ART-03 (Episode thumbnail from built-in), ART-04 (graceful no-image fallback), and PLUG-03 (config page renders and a saved change survives a server restart).

Purpose: ART-01 and ART-03 are NOT covered by any plugin code — they are Jellyfin built-in behavior. The ONLY way to satisfy those requirements is a live confirmation that the built-in providers pick up `poster.jpg` (Series Primary) and `<videofilename>.jpg` (Episode Primary). ART-02 and PLUG-03 also need live confirmation that the custom provider and config persistence actually work end-to-end.
Output: Committed image fixtures + a fixture-generation script, updated harness README, and a verified live confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-artwork-config-page/03-RESEARCH.md
@test/jellyfin-load-test/README.md
@test/jellyfin-load-test/docker-compose.yml

<dependency_note>
This plan depends on 03-01 (backdrop provider) and 03-02 (config page) being built — the deployed DLL must contain both.

ART-03 (Episode thumbnail) also depends on **Phase 2** being executed and deployed, because Episodes must exist for an episode image to attach. As of this plan's authoring, Phase 2 has NOT been executed (no Phase 2 SUMMARYs on disk; the harness media folder contains only `MyChannel/test_video.{mp4,nfo}` and `__kids/`, with no year-season episode fixtures). The executor MUST detect this at runtime:
- If Episodes are present in the live library (Phase 2 deployed), verify ART-03 fully.
- If Episodes are NOT present, ART-03 cannot be confirmed in this run. Place a per-video thumbnail fixture next to the existing video anyway, record ART-03 as DEFERRED-PENDING-PHASE-2 in the SUMMARY, and surface this clearly in the checkpoint so the operator is not asked to verify something that cannot exist. Do NOT fail the plan for this — ART-01/ART-02/ART-04/PLUG-03 are independently verifiable now.
</dependency_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate image fixtures and stage the built plugin DLL</name>
  <read_first>
    - test/jellyfin-load-test/README.md (existing deploy + run instructions, deploy-plugin.sh usage, media layout — note actual folders are MyChannel and __kids, NOT ChannelA/ChannelB as RESEARCH assumed)
    - test/jellyfin-load-test/docker-compose.yml (volume mounts: media dir, plugins dir, config dir)
    - .planning/phases/03-artwork-config-page/03-RESEARCH.md ("New Fixtures Required", "Creating minimal JPEG fixtures", image-cache Pitfall 2)
  </read_first>
  <action>Create test/jellyfin-load-test/scripts/make-image-fixtures.sh: a bash script that writes valid minimal JPEG fixtures into the harness media tree using a base64-decoded tiny (sub-1KB) valid JPEG blob (do NOT rely on ImageMagick — the script must work with only base64 + bash, since the operator's environment may lack convert). The script writes: (1) test/jellyfin-load-test/media/MyChannel/poster.jpg (channel art → ART-01/ART-02); (2) a per-video thumbnail next to the existing video — match the existing video basename exactly so the built-in EpisodeLocalImageProvider finds it (read README/media to get the real basename, e.g. test_video.jpg if the video is test_video.mp4) → ART-03. The script MUST NOT touch __kids (it must remain poster-less to exercise ART-04 graceful degradation). Run the script once so poster.jpg (and the thumbnail) exist and are committed. Then build and stage the plugin DLL exactly as the README's deploy step prescribes (dotnet build/publish -c Release of Jellyfin.Plugin.Youtarr, copy the published Jellyfin.Plugin.Youtarr.dll into the harness plugins directory) so the running container picks up the 03-01 backdrop provider and the 03-02 config page. Verify the staged DLL is the freshly built one (timestamp/size sanity). Append an "Artwork + Config Verification" section to README.md documenting fixtures, the refresh-images curl, and the per-requirement PASS checks. Do NOT run any sudo or docker command in this task — only file generation, dotnet build, and copy.</action>
  <verify>
    <automated>test -f test/jellyfin-load-test/scripts/make-image-fixtures.sh && test -f test/jellyfin-load-test/media/MyChannel/poster.jpg && head -c 2 test/jellyfin-load-test/media/MyChannel/poster.jpg | od -An -tx1 | grep -q "ff d8" && dotnet build Jellyfin.Plugin.Youtarr/Jellyfin.Plugin.Youtarr.csproj -c Release 2>&1 | grep -qiE "Build succeeded|0 Error" && echo OK</automated>
  </verify>
  <done>make-image-fixtures.sh exists and is committed; MyChannel/poster.jpg exists and begins with a valid JPEG SOI marker (FF D8); a per-video thumbnail matching the existing video basename exists; __kids has no poster.jpg; the freshly built plugin DLL is staged in the harness plugins directory; README documents the verification procedure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live artwork + config verification in Jellyfin (operator runs Docker)</name>
  <read_first>
    - test/jellyfin-load-test/README.md (the Artwork + Config Verification section added in Task 1)
    - .planning/phases/03-artwork-config-page/03-RESEARCH.md ("Live Verification Approach" — verification curls; Pitfall 2 image-cache staleness)
  </read_first>
  <what-built>
    - Custom YoutarrSeriesImageProvider surfacing poster.jpg as the Series Backdrop (ART-02), explicitly DI-registered.
    - Fleshed-out configPage.html with year-seasons checkbox, episode-numbering select, and max-description-length input, wired to ApiClient (PLUG-03).
    - Harness image fixtures: MyChannel/poster.jpg (+ a per-video thumbnail) committed; __kids deliberately poster-less for ART-04.
    - The freshly built plugin DLL staged in the harness plugins directory.
  </what-built>
  <how-to-verify>
    The Docker daemon needs root — the agent cannot start it. Run these yourself:

    1) Start the Docker daemon (only if not already running):
       sudo systemctl start docker

    2) From the repo root, bring up the harness (rebuild not needed — DLL + fixtures are mounted):
       cd test/jellyfin-load-test && docker compose up -d && docker compose logs -f
       (Ctrl-C to stop following logs once Jellyfin reports it is listening on 8096.)

    3) Open http://localhost:8096 and complete first-run setup if prompted. Create/point a "Shows" library at the mounted media directory (per README).

    4) Force a full image refresh so cached "no images" state is cleared (Pitfall 2). In the Dashboard, on the MyChannel Series choose "Refresh metadata → Replace all images", OR run the refresh curl documented in the README with an admin API key. Wait ~30-60s for the scan.

    5) ART-01 (Series Primary): In the library grid, MyChannel shows the poster.jpg as its poster image. PASS = poster visible on the card.

    6) ART-02 (Series Backdrop): Open the MyChannel detail page. A backdrop image (the poster.jpg, likely letter-boxed because it is portrait — this is the documented intentional tradeoff) appears behind the header. PASS = a backdrop is present, not a blank/black header.

    7) ART-03 (Episode thumbnail): IF Phase 2 is deployed and Episodes exist, open an Episode and confirm its card/primary image is the per-video thumbnail. If NO Episodes exist (Phase 2 not yet run), the agent will have flagged ART-03 as DEFERRED-PENDING-PHASE-2 — skip this step; it will be verified when Phase 2 ships.

    8) ART-04 (graceful degradation): Confirm the __kids-derived item (no poster.jpg) and any thumbnail-less episode display with NO image and NO error — the library scan completes cleanly. Optionally check the container log for new exceptions:
       docker exec jellyfin-plugin-test sh -c 'grep -i "exception\|error" /config/log/log_*.log | tail -20'
       PASS = no new artwork-related exceptions; items still resolve.

    9) PLUG-03 (config page + persistence): Dashboard → Plugins → YoutarrMetadata → Settings. PASS = all three controls render (year-seasons checkbox, numbering select with Default/YYYYMMDD, max-description-length number). Change Max Description Length to a distinct value (e.g. 250), click Save. Then restart the server:
       cd test/jellyfin-load-test && docker compose restart
       Reopen the config page after restart. PASS = the value is still 250 (persisted across restart).

    10) Tear down when done:
        cd test/jellyfin-load-test && docker compose down
  </how-to-verify>
  <resume-signal>Type "approved" if ART-01, ART-02, ART-04, and PLUG-03 all pass (and ART-03 either passes or is correctly flagged deferred until Phase 2). Otherwise describe which check failed and what you observed (e.g. backdrop blank, config value not retained, exception in log).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → Docker daemon | sudo required to start the daemon; surfaced as a copy-paste command, never run by the agent |
| on-disk media files → Jellyfin scanner | poster.jpg / thumbnail paths consumed read-only by built-in + custom providers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | poster.jpg path handling in YoutarrSeriesImageProvider | mitigate | Path.Combine(item.Path, "poster.jpg") with a constant filename — no user-controlled path segment; item.Path is Jellyfin-canonicalized (verified live here) |
| T-03-05 | Denial of Service | image scan over missing-file channels (__kids) | mitigate | Provider yields empty on File.Exists==false; live check step 8 confirms no exception and scan completion |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in Phase 3 — RESEARCH Package Legitimacy Audit confirms zero new packages; no install checkpoint required |
</threat_model>

<verification>
- Task 1 automated checks pass (fixtures present, valid JPEG SOI, plugin builds).
- Operator confirms ART-01, ART-02, ART-04, and PLUG-03 (with restart persistence) pass live; ART-03 passes if Phase 2 deployed, else recorded DEFERRED-PENDING-PHASE-2.
- No new artwork-related exceptions in the Jellyfin container log.
</verification>

<success_criteria>
- ART-01 confirmed: built-in LocalImageProvider surfaces poster.jpg as Series Primary live.
- ART-02 confirmed: custom YoutarrSeriesImageProvider surfaces poster.jpg as Series Backdrop live.
- ART-03 confirmed live if Episodes exist, otherwise explicitly deferred to Phase 2 with a thumbnail fixture pre-staged.
- ART-04 confirmed: missing-artwork channels/episodes degrade gracefully with no errors.
- PLUG-03 confirmed: config page renders all three controls and a saved change persists across a server restart.
</success_criteria>

<output>
Create `.planning/phases/03-artwork-config-page/03-03-SUMMARY.md` when done. Record the ART-03 disposition (verified vs DEFERRED-PENDING-PHASE-2) explicitly.
</output>
