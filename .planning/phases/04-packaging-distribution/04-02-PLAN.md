---
phase: 04-packaging-distribution
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: false
requirements: [PLUG-02]
user_setup: []

must_haves:
  truths:
    - "The dist ZIP, extracted into the Jellyfin plugins directory, loads as Active after restart"
    - "The extracted plugin folder contains meta.json + Jellyfin.Plugin.Youtarr.dll matching a real end-user install"
    - "The Jellyfin log reports the plugin version 1.0.0.0 is compatible with the server"
  artifacts:
    - path: "test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/meta.json"
      provides: "Extracted ZIP install (meta.json present — matches real install, not just the DLL deploy path)"
    - path: "test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/Jellyfin.Plugin.Youtarr.dll"
      provides: "Plugin DLL from the distribution ZIP"
  key_links:
    - from: "dist/youtarrmetadata_1.0.0.0.zip"
      to: "test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/"
      via: "unzip -o extract (flat) into the versioned plugin folder"
      pattern: "unzip.*youtarrmetadata.*-d.*YoutarrMetadata_1.0.0.0"
---

<objective>
Verify the PLUG-02 manual-install path end-to-end: extract the distribution ZIP produced in Plan 04-01 into the Jellyfin plugins directory exactly as an end user would, restart the Jellyfin container, and confirm the plugin loads as "Active". This closes the packaging loop — the same ZIP a user downloads is proven to install and load.

Purpose: PKG-01/PKG-02 prove the ZIP and manifest are well-formed; this plan proves the ZIP actually installs into a real Jellyfin 10.10.x server. It is the user-facing acceptance of the install path.
Output: The ZIP extracted into test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/ (flat meta.json + DLL) and a human-verified "Active" status in the Jellyfin Dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-packaging-distribution/04-RESEARCH.md
@.planning/phases/04-packaging-distribution/04-01-PLAN.md
@test/jellyfin-load-test/README.md
@scripts/deploy-plugin.sh

<interfaces>
<!-- Depends on Plan 04-01 having produced dist/youtarrmetadata_1.0.0.0.zip -->

Manual install convention (RESEARCH "Manual Install Flow"):
  Target folder: test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/
    (folder name = build.yaml name_version "YoutarrMetadata_1.0.0.0", NOT the slug —
     same convention deploy-plugin.sh already uses and that worked in Phase 1 PLUG-01)
  Extract ZIP flat into that folder -> meta.json + Jellyfin.Plugin.Youtarr.dll

Extract command (no sudo, no docker):
  mkdir -p test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0
  unzip -o dist/youtarrmetadata_1.0.0.0.zip -d test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/
  (Fallback if unzip absent: python3 -m zipfile -e dist/youtarrmetadata_1.0.0.0.zip test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/)

Operator-only step (requires sudo to start the Docker daemon — surface, never run):
  sudo systemctl start docker        # operator runs this
  docker restart jellyfin-plugin-test
Expected Jellyfin log line:
  Plugin 'YoutarrMetadata' version '1.0.0.0' is compatible with this server.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the distribution ZIP into the harness plugins directory</name>
  <files>test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/meta.json, test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/Jellyfin.Plugin.Youtarr.dll</files>
  <read_first>
    - .planning/phases/04-packaging-distribution/04-RESEARCH.md (sections "Manual Install Flow (PLUG-02)" and "Install Verification Approach")
    - scripts/deploy-plugin.sh (folder-naming convention YoutarrMetadata_1.0.0.0 that already works)
    - test/jellyfin-load-test/README.md (harness operation, plugins dir layout)
  </read_first>
  <action>
    Confirm dist/youtarrmetadata_1.0.0.0.zip exists (produced by Plan 04-01); if missing, stop and instruct the operator to run ./scripts/package.sh first.

    Create the versioned plugin folder test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/ and extract the ZIP flat into it: `unzip -o dist/youtarrmetadata_1.0.0.0.zip -d test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/`. If unzip is unavailable, fall back to `python3 -m zipfile -e dist/youtarrmetadata_1.0.0.0.zip test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/`.

    This overwrites any DLL left by deploy-plugin.sh and adds meta.json, so the folder now matches exactly what a real end-user manual install looks like (DLL + meta.json), not the DLL-only dev-deploy path. The plugins/ directory is gitignored — do not commit it. Do NOT run docker or sudo here.
  </action>
  <verify>
    <automated>test -f test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/meta.json && test -f test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/Jellyfin.Plugin.Youtarr.dll && python3 -c "import json; d=json.load(open('test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/meta.json')); assert d.get('guid')=='80302d7f-7fc3-4b1c-9a3f-fd85b98b9a69'; assert d.get('version')=='1.0.0.0'"</automated>
  </verify>
  <acceptance_criteria>
    - test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/ contains both meta.json and Jellyfin.Plugin.Youtarr.dll (flat, no subdir).
    - meta.json guid == 80302d7f-7fc3-4b1c-9a3f-fd85b98b9a69 and version == 1.0.0.0.
    - No sudo or docker was invoked by the agent; plugins/ not committed.
  </acceptance_criteria>
  <done>The distribution ZIP is extracted into the harness plugins directory as a real end-user install (meta.json + DLL), ready for the live restart check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification — plugin installs from ZIP and loads as Active</name>
  <what-built>
    The distribution ZIP (dist/youtarrmetadata_1.0.0.0.zip from Plan 04-01) has been extracted into test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/ as a flat meta.json + DLL — identical to a real end-user manual install. The Docker harness now needs the daemon started and the container restarted (both require operator action: the Docker daemon needs sudo to start, per project policy the agent never runs sudo or docker).
  </what-built>
  <how-to-verify>
    1. Start the Docker daemon (operator, sudo):
         sudo systemctl start docker
    2. Bring up / restart the harness from the repo root:
         cd test/jellyfin-load-test
         docker compose up -d            # if not already running
         docker restart jellyfin-plugin-test
    3. Wait ~20s for Jellyfin to start, then check the log for the compatibility line:
         docker exec jellyfin-plugin-test sh -c 'grep -r "YoutarrMetadata\|Youtarr" /config/log/ 2>/dev/null'
       Expected: "Plugin 'YoutarrMetadata' version '1.0.0.0' is compatible with this server."
    4. In a browser, open the Jellyfin Dashboard (http://localhost:8096) → Dashboard → Plugins.
       Confirm "YoutarrMetadata" appears with status "Active" and version 1.0.0.0, with NO load errors.

    This proves PLUG-02: a downloaded ZIP, extracted into the plugins directory and the server restarted, installs and loads cleanly.
  </how-to-verify>
  <resume-signal>Type "approved" if the plugin shows "Active" 1.0.0.0 with no load errors, or describe what failed (load error, wrong version, plugin missing).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ZIP → Jellyfin plugin loader | The ZIP is developer-built end-to-end (Plan 04-01). Extracting and loading it in the harness introduces no untrusted input. The Docker harness is a local, disposable test environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | extracted plugin DLL/meta.json | accept | Artifacts originate from the developer-controlled Plan 04-01 build; no third-party code is introduced. Verification is a read-only Active-status check. |
| T-04-04 | Elevation of Privilege | Docker daemon start | accept | Daemon start requires operator sudo and is surfaced as a command, never executed by the agent (project policy: agents never run sudo/docker). |
</threat_model>

<verification>
- The distribution ZIP extracts into test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/ as flat meta.json + DLL with the correct guid/version.
- After the operator starts Docker and restarts the container, the Jellyfin Dashboard shows YoutarrMetadata 1.0.0.0 as "Active" with no load errors and the log reports version compatibility.
</verification>

<success_criteria>
- PLUG-02: A user can install the plugin by dropping the downloadable ZIP into the Jellyfin plugins directory (extract into YoutarrMetadata_1.0.0.0/) and restarting — verified live as "Active" in the harness.
</success_criteria>

<output>
Create `.planning/phases/04-packaging-distribution/04-02-SUMMARY.md` when done.
</output>
