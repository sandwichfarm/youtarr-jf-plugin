---
phase: 02-episodes-year-seasons
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-03"]
files_modified:
  - Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProbeStub.cs
  - Jellyfin.Plugin.Youtarr/PluginServiceRegistrator.cs
  - test/jellyfin-load-test/README.md
autonomous: false
requirements: [LIB-02, LIB-03, LIB-04, EPI-01, EPI-07, CMP-01, CMP-02]
must_haves:
  truths:
    - "The throwaway probe stub is deleted; the real YoutarrEpisodeNfoProvider drives episode metadata in live Jellyfin"
    - "ChannelA videos appear as Episodes under virtual Season 2023 and Season 2024 with title/plot/date/runtime from their NFOs"
    - "ChannelB missing-date video appears under Season 0 and does not crash the scan (EPI-07)"
    - "A nested-layout video resolves as an Episode identically to a flat-layout one (CMP-02)"
  artifacts:
    - path: "Jellyfin.Plugin.Youtarr/PluginServiceRegistrator.cs"
      provides: "Explicit episode-provider DI registration only if 02-01 recorded auto-discovery failure"
      contains: "RegisterServices"
  key_links:
    - from: "YoutarrEpisodeNfoProvider"
      to: "live Jellyfin scan"
      via: "auto-discovered ILocalMetadataProvider<Episode> populating Episodes"
      pattern: "ILocalMetadataProvider<Episode>"
---

<objective>
Close the Phase 2 vertical slice end-to-end: remove the Wave 0 throwaway stub, deploy the real `YoutarrEpisodeNfoProvider` (built and unit-proven in 02-03) to the live Docker Jellyfin 10.10.7 harness, and confirm — in the actual UI/API — that Youtarr videos appear as Episodes grouped into year-named virtual seasons with full per-video metadata, with the missing-date video landing gracefully in Season 0.

Purpose: Unit tests prove the mapping logic; only a live scan proves the whole pipeline (auto-discovery, ParentIndexNumber → virtual season, NFO co-location, nested layout) works against the real server. This is the MVP "proven live" gate for Phase 2.

Output: the stub deleted, the real provider live-verified, the runbook updated with the full-pipeline verification, and (only if 02-01 recorded auto-discovery failure) an explicit DI registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-episodes-year-seasons/02-RESEARCH.md
@.planning/phases/02-episodes-year-seasons/02-01-SUMMARY.md
@.planning/phases/02-episodes-year-seasons/02-03-SUMMARY.md
@Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProvider.cs
@Jellyfin.Plugin.Youtarr/PluginServiceRegistrator.cs
@scripts/deploy-plugin.sh
@test/jellyfin-load-test/README.md

<interfaces>
<!-- From 02-01: the probe outcome dictates the DI decision in Task 2. -->
02-01-SUMMARY records: virtual-season PASS/FAIL, and whether episode-provider DI auto-discovery worked
(any `[Youtarr]` episode log line). Read it before Task 2.

If 02-01 was FAIL (ParentIndexNumber discarded), STOP and surface the ILibraryPostScanTask fallback decision
to the operator — do NOT proceed to the full-pipeline live verify with a known-broken mechanism.

Explicit DI registration (only if auto-discovery failed), added to PluginServiceRegistrator.RegisterServices:
  serviceCollection.AddSingleton<ILocalMetadataProvider<Episode>, YoutarrEpisodeNfoProvider>();
  (ILocalMetadataProvider<T> from MediaBrowser.Controller.Providers; Episode from MediaBrowser.Controller.Entities.TV)

Provider-Order fallback (Open Question #2): if Episodes are reached but metadata is blank, add `public int Order => 0;`
to YoutarrEpisodeNfoProvider so it precedes the built-in EpisodeNfoProvider.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the probe stub; add a nested-layout fixture; deploy</name>
  <read_first>
    - .planning/phases/02-episodes-year-seasons/02-01-SUMMARY.md (probe outcome; DI auto-discovery result)
    - Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProbeStub.cs (the throwaway to remove)
    - test/jellyfin-load-test/media/ChannelA/ (existing flat fixtures from 02-01 to mirror for the nested case)
    - scripts/deploy-plugin.sh
  </read_first>
  <files>Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProbeStub.cs, test/jellyfin-load-test/README.md</files>
  <action>
    Delete Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProbeStub.cs — its only purpose was the Wave 0 mechanism probe, now superseded by the real provider.
    Add ONE nested-layout fixture to exercise CMP-02: create test/jellyfin-load-test/media/ChannelA/Nested Video 2024 [ytid4]/ containing an empty `Nested Video 2024 [ytid4].mp4` and a co-located `Nested Video 2024 [ytid4].nfo` (`<movie>` root, title "Nested 2024 Video", studio "Channel A", premiered 2024-08-01, plot, uniqueid type="youtube" ytid4, runtime 4). This proves the same provider handles the per-video-subfolder layout.
    Rebuild Release to confirm no reference to the deleted stub remains, then run `./scripts/deploy-plugin.sh` to publish + stage the updated DLL into the harness plugins folder. (Agent runs the deploy script; it needs no docker/sudo.)
  </action>
  <verify>
    <automated>test ! -f Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProbeStub.cs && test -f "test/jellyfin-load-test/media/ChannelA/Nested Video 2024 [ytid4]/Nested Video 2024 [ytid4].nfo" && dotnet build Jellyfin.Plugin.Youtarr/Jellyfin.Plugin.Youtarr.csproj -c Release && test -f test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/Jellyfin.Plugin.Youtarr.dll</automated>
  </verify>
  <acceptance_criteria>
    - YoutarrEpisodeNfoProbeStub.cs no longer exists; Release build succeeds with no dangling reference.
    - A nested-layout video fixture exists under ChannelA/Nested Video 2024 [ytid4]/ with co-located mp4 + nfo.
    - deploy-plugin.sh has staged a fresh Jellyfin.Plugin.Youtarr.dll into YoutarrMetadata_1.0.0.0/.
  </acceptance_criteria>
  <done>The real provider is staged for live deployment; the throwaway stub is gone and a nested-layout case is present.</done>
</task>

<task type="auto">
  <name>Task 2: Apply explicit episode-provider DI registration only if the probe required it</name>
  <read_first>
    - .planning/phases/02-episodes-year-seasons/02-01-SUMMARY.md (did auto-discovery produce [Youtarr] episode log lines?)
    - Jellyfin.Plugin.Youtarr/PluginServiceRegistrator.cs (existing IResolverIgnoreRule registration to extend)
    - .planning/phases/02-episodes-year-seasons/02-RESEARCH.md (Pitfall 7 explicit-registration safety net, lines ~927-938)
  </read_first>
  <files>Jellyfin.Plugin.Youtarr/PluginServiceRegistrator.cs</files>
  <action>
    Conditional on the 02-01 probe record:
      - If 02-01 reported that the episode provider was auto-discovered (`[Youtarr]` episode log lines appeared) — make NO code change; instead update the class XML-doc in PluginServiceRegistrator.cs to note that ILocalMetadataProvider<Episode> is confirmed auto-discovered (no explicit registration needed), keeping this task a documented no-op.
      - If 02-01 reported the provider was NOT discovered (no episode log lines / metadata blank) — add `serviceCollection.AddSingleton<ILocalMetadataProvider<Episode>, YoutarrEpisodeNfoProvider>();` to RegisterServices, with the needed usings (MediaBrowser.Controller.Providers, MediaBrowser.Controller.Entities.TV, Jellyfin.Plugin.Youtarr.Providers). Rebuild Release and re-run deploy-plugin.sh.
    Do not register the provider speculatively when auto-discovery already works — redundant registration is the safety net only.
  </action>
  <verify>
    <automated>dotnet build Jellyfin.Plugin.Youtarr/Jellyfin.Plugin.Youtarr.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - Decision matches the 02-01 record: explicit AddSingleton present iff auto-discovery failed; otherwise a doc-only note and no functional change.
    - Plugin builds clean in Release.
  </acceptance_criteria>
  <done>Episode-provider DI is correct for the live environment per the recorded probe result.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The real `YoutarrEpisodeNfoProvider` (full NFO→Episode mapping with year-seasons, numbering, and Season-0 fallback) is deployed in place of the deleted probe stub, with flat fixtures (ChannelA 2023 + 2024, ChannelB missing-date) and a nested fixture (ChannelA/Nested Video 2024). The agent has run `./scripts/deploy-plugin.sh`.
  </what-built>
  <how-to-verify>
    Docker needs sudo to start — the agent cannot. Copy-paste as the operator:

    1. Restart with a clean scan:
       sudo systemctl start docker
       rm -rf test/jellyfin-load-test/config test/jellyfin-load-test/cache
       cd test/jellyfin-load-test && docker compose up -d   # wait ~20s

    2. Browser http://localhost:8096: create admin, add a "Shows" library at /media, DISABLE "Save metadata to media folders", disable TVDB + TMDB. Get an admin API key.

    3. Run the "Phase 2 Full-Pipeline Verify" block from test/jellyfin-load-test/README.md:
       - POST /Library/Refresh; wait ~30s.
       - GET /Shows → note the ChannelA and ChannelB series ids.
       - GET /Shows/{ChannelA-id}/Seasons → expect virtual "Season 2023" AND "Season 2024" (no physical folders).
       - GET /Shows/{ChannelA-id}/Episodes → expect the 2023 video under Season 2023, the 2024 flat video AND the nested video under Season 2024, each with Name/Overview/PremiereDate/RunTimeTicks populated from its NFO.
       - GET /Shows/{ChannelB-id}/Seasons → expect "Season 0"/Specials holding the missing-date video; the scan did not crash.
       - Open one episode in the metadata editor: confirm the YouTube id shows as a provider id (EPI-03) and content rating if the NFO has `<mpaa>`.
       - docker exec jellyfin-plugin-test grep -r "\[Youtarr\]" /config/log/ → no exceptions.

    PASS criteria (record each): ChannelA Season 2023 + Season 2024 virtual containers (LIB-03); correct episode-under-correct-season placement including the nested video (LIB-02, CMP-01, CMP-02); per-video title/plot/date/runtime populated (EPI-01); ChannelB video in Season 0 with no scan crash (EPI-07).
    If metadata is blank despite Episodes existing: add `public int Order => 0;` to YoutarrEpisodeNfoProvider (Open Question #2), redeploy, rescan.

    In your reply, record PASS/FAIL per requirement and any Order tweak applied.
  </how-to-verify>
  <resume-signal>Type "approved — episodes + year-seasons live" with the recorded per-requirement results, or describe the failure observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record the full-pipeline verify procedure in the runbook</name>
  <read_first>
    - test/jellyfin-load-test/README.md (Phase 2 Wave 0 section added in 02-01)
    - .planning/phases/02-episodes-year-seasons/02-RESEARCH.md (verification commands + pass-criteria table, lines ~999-1043)
  </read_first>
  <files>test/jellyfin-load-test/README.md</files>
  <action>
    Add a "## Phase 2 Full-Pipeline Verify" section documenting the exact operator sequence used in the Task 3 checkpoint: clean-scan restart, the /Shows + /Shows/{id}/Seasons + /Shows/{id}/Episodes curl block (reusing the README's API_KEY pattern), the metadata-editor check for YouTube provider id + content rating, the per-requirement pass table (LIB-02/03/04, EPI-01, EPI-07, CMP-01/02), and the `Order => 0` remedy for blank metadata. Append after the existing Wave 0 section; do not duplicate it.
  </action>
  <verify>
    <automated>grep -q "Full-Pipeline" test/jellyfin-load-test/README.md && grep -q "Episodes" test/jellyfin-load-test/README.md && grep -q "Season 0" test/jellyfin-load-test/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README has a Phase 2 Full-Pipeline Verify section with the Seasons + Episodes curl commands and per-requirement pass table.
    - The metadata-editor provider-id/rating check and the Order=>0 remedy are documented.
  </acceptance_criteria>
  <done>The operator runbook documents how to independently re-verify the full episode pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NFO files → live provider | Untrusted on-disk XML parsed during a real Jellyfin scan |
| operator → Docker daemon | sudo required to start daemon; agent never runs sudo |
| Jellyfin metadata saver → media | server could write NFOs back over user media if misconfigured |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | Jellyfin overwriting Youtarr NFOs (issue #12197) | mitigate | media mounted `:ro`; README reiterates disabling "Save metadata to media folders" |
| T-02-13 | Elevation of Privilege | sudo for Docker daemon | transfer | Copy-paste sudo command surfaced to operator; agent never runs sudo |
| T-02-14 | Denial of Service | missing-date video crashing live scan | mitigate | EPI-07 Season-0 fallback (02-03), exercised live by ChannelB; grep confirms no scan exception |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to gate |
</threat_model>

<verification>
- Probe stub deleted; Release build clean; fresh DLL staged.
- Live scan: ChannelA shows Season 2023 + Season 2024 with correctly-placed Episodes (flat + nested) carrying full metadata; ChannelB video in Season 0 with no crash.
- DI registration matches the 02-01 auto-discovery record.
</verification>

<success_criteria>
- In live Jellyfin 10.10.7, Youtarr videos appear as Episodes under year-named virtual seasons with title/plot/date/runtime/genres/tags/YouTube-id/rating populated from their NFOs.
- The missing-date video lands in Season 0 without crashing the scan (EPI-07).
- Both flat and nested layouts resolve identically (CMP-01/CMP-02) — the Phase 2 MVP slice is proven live.
</success_criteria>

<output>
Create `.planning/phases/02-episodes-year-seasons/02-04-SUMMARY.md` when done. Record the live per-requirement results and any provider-Order tweak applied.
</output>
