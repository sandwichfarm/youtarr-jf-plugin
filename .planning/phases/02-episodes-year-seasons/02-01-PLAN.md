---
phase: 02-episodes-year-seasons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProbeStub.cs
  - test/jellyfin-load-test/media/ChannelA/Video 2023 [ytid1].nfo
  - test/jellyfin-load-test/media/ChannelA/Video 2023 [ytid1].mp4
  - test/jellyfin-load-test/media/ChannelA/Video 2024 [ytid2].nfo
  - test/jellyfin-load-test/media/ChannelA/Video 2024 [ytid2].mp4
  - test/jellyfin-load-test/media/ChannelB/Missing Date [ytid3].nfo
  - test/jellyfin-load-test/media/ChannelB/Missing Date [ytid3].mp4
  - test/jellyfin-load-test/README.md
autonomous: false
requirements: [LIB-02, LIB-03]
must_haves:
  truths:
    - "A stub Episode provider that hardcodes ParentIndexNumber=2024 causes a virtual 'Season 2024' container to appear under a flat-layout channel Series in live Jellyfin 10.10.7"
    - "Episode items appear under the year-named virtual Season with no physical Season folder on disk"
    - "The probe outcome (pass = build full pipeline; fail = ILibraryPostScanTask fallback) is recorded for downstream plans"
  artifacts:
    - path: "Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProbeStub.cs"
      provides: "Minimal ILocalMetadataProvider<Episode> hardcoding ParentIndexNumber=2024 + IndexNumber=1"
      contains: "ParentIndexNumber = 2024"
    - path: "test/jellyfin-load-test/media/ChannelA/Video 2024 [ytid2].nfo"
      provides: "Episode fixture for the live virtual-season probe"
      contains: "<movie>"
  key_links:
    - from: "YoutarrEpisodeNfoProbeStub"
      to: "Jellyfin SeriesMetadataService.CreateSeasonsAsync"
      via: "Episode.ParentIndexNumber set in MetadataResult"
      pattern: "ParentIndexNumber"
---

<objective>
Front-load the single highest-risk validation of Phase 2: confirm that setting `Episode.ParentIndexNumber` on a flat-layout channel folder causes Jellyfin 10.10.7 to auto-create a virtual "Season YYYY" container — BEFORE the full NFO pipeline is built. This is the Wave 0 live probe mandated by 02-RESEARCH.md (assumption A3, the only HIGH-risk assumption).

Purpose: If virtual-season creation does not work in the live 10.10.7 binary, the entire year-season approach must switch to an `ILibraryPostScanTask` fallback. Discovering this now (with a one-file stub) costs minutes; discovering it after building the full provider costs the whole phase.

Output: A throwaway stub provider, episode/year fixtures added to the load-test harness, an updated runbook section, and a recorded live human-verify result that unblocks (or redirects) all later Phase 2 plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-episodes-year-seasons/02-RESEARCH.md
@.planning/phases/01-scaffold-series-proof/01-03-SUMMARY.md
@Jellyfin.Plugin.Youtarr/Providers/YoutarrSeriesNfoProvider.cs
@Jellyfin.Plugin.Youtarr/Constants.cs
@scripts/deploy-plugin.sh

<interfaces>
<!-- Executor uses these directly; no codebase exploration needed. -->

Constants (Jellyfin.Plugin.Youtarr/Constants.cs):
  Constants.ProviderName == "Youtarr"

ILocalMetadataProvider<Episode> shape (matches existing YoutarrSeriesNfoProvider, swap Series→Episode):
  - namespace MediaBrowser.Controller.Providers: ILocalMetadataProvider<T>, MetadataResult<T>, ItemInfo, IDirectoryService
  - namespace MediaBrowser.Controller.Entities.TV: Episode
  - string Name => Constants.ProviderName;
  - Task<MetadataResult<Episode>> GetMetadata(ItemInfo info, IDirectoryService directoryService, CancellationToken cancellationToken)
  - result.HasMetadata = true is REQUIRED or Jellyfin discards the result (Pitfall 1)

ItemInfo construction (from YoutarrSeriesNfoProviderTests, for reference): no parameterless ctor in 10.10.7.

Episode properties used by the stub: Name (string), ParentIndexNumber (int?), IndexNumber (int?).

Deploy loop (scripts/deploy-plugin.sh): publishes Release + stages DLL into
test/jellyfin-load-test/plugins/YoutarrMetadata_1.0.0.0/. Agent runs the script; operator runs Docker (sudo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add episode + year fixtures to the load-test harness</name>
  <read_first>
    - .planning/phases/02-episodes-year-seasons/02-RESEARCH.md ("Wave 0: Virtual Season Probe" fixtures section, lines ~947-997)
    - .planning/phases/01-scaffold-series-proof/01-03-SUMMARY.md (existing harness layout, media mounted :ro)
    - test/jellyfin-load-test/media/MyChannel/test_video.nfo (existing NFO fixture shape to mirror)
  </read_first>
  <files>test/jellyfin-load-test/media/ChannelA/Video 2023 [ytid1].nfo, test/jellyfin-load-test/media/ChannelA/Video 2023 [ytid1].mp4, test/jellyfin-load-test/media/ChannelA/Video 2024 [ytid2].nfo, test/jellyfin-load-test/media/ChannelA/Video 2024 [ytid2].mp4, test/jellyfin-load-test/media/ChannelB/Missing Date [ytid3].nfo, test/jellyfin-load-test/media/ChannelB/Missing Date [ytid3].mp4</files>
  <action>
    Create two new flat-layout channel folders under test/jellyfin-load-test/media/.
    ChannelA holds two videos in different upload years; ChannelB holds one video with an empty premiered date (EPI-07 fixture).
    For each video create an empty .mp4 (touch) and a co-located same-basename .nfo with a `<movie>` root.
    The `[ytidN]` bracket suffix in the filenames is intentional — it confirms Youtarr's `[YouTubeID]` filename pattern does NOT trigger Jellyfin's SxxExx parser (assumption A1).
    NFO field values exactly as specified in 02-RESEARCH.md "Wave 0" section:
      - Video 2023 [ytid1].nfo: title "2023 Video", studio "Channel A", premiered 2023-06-15, plot present, uniqueid type="youtube" ytid1, runtime 5.
      - Video 2024 [ytid2].nfo: title "2024 Video", studio "Channel A", premiered 2024-03-20, plot present, uniqueid type="youtube" ytid2, runtime 7.
      - Missing Date [ytid3].nfo: title "Missing Date Video", studio "Channel B", EMPTY premiered element (`<premiered></premiered>`), plot present, uniqueid type="youtube" ytid3.
    Each NFO must start with the XML declaration `<?xml version="1.0" encoding="utf-8"?>` and be written as UTF-8.
    Do NOT delete the existing MyChannel or __kids fixtures.
  </action>
  <verify>
    <automated>test -f "test/jellyfin-load-test/media/ChannelA/Video 2024 [ytid2].nfo" && test -f "test/jellyfin-load-test/media/ChannelB/Missing Date [ytid3].mp4" && grep -l "2024-03-20" "test/jellyfin-load-test/media/ChannelA/Video 2024 [ytid2].nfo" && grep -c "<premiered></premiered>" "test/jellyfin-load-test/media/ChannelB/Missing Date [ytid3].nfo"</automated>
  </verify>
  <acceptance_criteria>
    - 6 fixture files exist (3 nfo + 3 mp4) across ChannelA and ChannelB.
    - ChannelA/Video 2023 nfo contains premiered 2023-06-15; ChannelA/Video 2024 nfo contains premiered 2024-03-20.
    - ChannelB/Missing Date nfo contains an empty `<premiered></premiered>` element.
    - All NFOs are valid `<movie>`-rooted XML with a UTF-8 declaration.
    - Existing MyChannel and __kids fixtures are untouched.
  </acceptance_criteria>
  <done>Two new channel folders with year-differentiated and missing-date episode fixtures exist in the harness media tree.</done>
</task>

<task type="auto">
  <name>Task 2: Add the throwaway virtual-season probe stub provider</name>
  <read_first>
    - Jellyfin.Plugin.Youtarr/Providers/YoutarrSeriesNfoProvider.cs (exact ILocalMetadataProvider pattern, namespaces, HasMetadata)
    - Jellyfin.Plugin.Youtarr/Constants.cs
    - .planning/phases/02-episodes-year-seasons/02-RESEARCH.md ("Wave 0" stub skeleton, lines ~656-679; Pitfall 7 DI auto-discovery)
  </read_first>
  <files>Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProbeStub.cs</files>
  <action>
    Create class YoutarrEpisodeNfoProbeStub implementing `ILocalMetadataProvider<Episode>` in namespace Jellyfin.Plugin.Youtarr.Providers.
    `Name => Constants.ProviderName`.
    GetMetadata returns a MetadataResult&lt;Episode&gt; whose Item is a new Episode with:
      Name = Path.GetFileNameWithoutExtension(info.Path), ParentIndexNumber = 2024, IndexNumber = 1, and result.HasMetadata = true.
    It reads NO NFO file — it is a deliberate hardcoded probe of the virtual-season mechanism only.
    Mark the class with a clear XML-doc comment that it is a THROWAWAY probe to be DELETED in plan 02-04 once the full YoutarrEpisodeNfoProvider replaces it.
    Use the same using-directives as YoutarrSeriesNfoProvider (Episode from MediaBrowser.Controller.Entities.TV; ItemInfo/IDirectoryService/MetadataResult/ILocalMetadataProvider from MediaBrowser.Controller.Providers) plus System.IO and System.Threading.
    Do NOT implement IHasItemChangeMonitor (not needed for the probe).
    Do NOT add explicit DI registration yet — rely on Jellyfin auto-discovery (Pitfall 7); the safety-net registration is decided by the live result in Task 3.
    Confirm the plugin still publishes clean (no MediaBrowser.* DLLs) via the deploy script.
  </action>
  <verify>
    <automated>dotnet build Jellyfin.Plugin.Youtarr/Jellyfin.Plugin.Youtarr.csproj -c Release && grep -q "ParentIndexNumber = 2024" Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProbeStub.cs && grep -q "result.HasMetadata = true\|HasMetadata = true" Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProbeStub.cs</automated>
  </verify>
  <acceptance_criteria>
    - Plugin builds clean in Release with the new stub.
    - Stub sets ParentIndexNumber = 2024, IndexNumber = 1, HasMetadata = true.
    - Stub class XML-doc explicitly marks it as a throwaway to be removed in 02-04.
    - No explicit DI registration added (auto-discovery path under test).
    - `dotnet publish -c Release` output still contains only Jellyfin.Plugin.Youtarr.dll (no MediaBrowser.* runtime DLLs).
  </acceptance_criteria>
  <done>A minimal hardcoded Episode provider builds into the plugin and is ready for live deployment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A throwaway stub Episode provider (`YoutarrEpisodeNfoProbeStub`) that hardcodes `ParentIndexNumber = 2024` for every video, plus ChannelA (2023 + 2024 videos) and ChannelB (missing-date video) fixtures. The agent has run `./scripts/deploy-plugin.sh` to stage the updated DLL. This validates assumption A3 (virtual season creation in live 10.10.7) before the full pipeline is built.
  </what-built>
  <how-to-verify>
    The Docker daemon requires sudo to start — the agent cannot run it. Copy-paste these commands as the operator:

    1. Start Docker + bring the harness up (wipe prior state first so the scan is clean):
       sudo systemctl start docker
       rm -rf test/jellyfin-load-test/config test/jellyfin-load-test/cache
       docker pull jellyfin/jellyfin:10.10.7
       cd test/jellyfin-load-test && docker compose up -d   # wait ~20s

    2. In the browser at http://localhost:8096: create the admin user, add a "Shows" library pointed at /media, DISABLE "Save metadata to media folders" (issue #12197), and disable TVDB + TMDB for that library. Trigger a library scan.

    3. Get an admin API key (Dashboard → API Keys), then run the verification block from the "Wave 0" section of test/jellyfin-load-test/README.md:
       - POST /Library/Refresh, wait ~30s
       - GET /Shows and find the ChannelA series id
       - GET /Shows/{ChannelA-id}/Seasons and inspect Name + IndexNumber
       - GET /Shows/{ChannelB-id}/Seasons for the missing-date channel
       - docker exec jellyfin-plugin-test grep -r "\[Youtarr\]" /config/log/

    PASS criteria (record each):
       - ChannelA appears as a Series.
       - A virtual "Season 2024" container exists under ChannelA with NO physical Season folder on disk, and both videos appear under it (the stub hardcodes 2024 for all).
       - The scan completes with no exception; ChannelB does not crash it.
    FAIL signals: episodes land in "Season 1" / "Unknown Season" (ParentIndexNumber discarded — issue #14080), or no `[Youtarr]` episode log lines (DI auto-discovery failed → explicit AddSingleton<ILocalMetadataProvider<Episode>, ...> needed in PluginServiceRegistrator).

    In your reply, record: PASS or FAIL; whether auto-discovery worked (any `[Youtarr]` episode log line); and if FAIL, which signal was observed (so 02-04 can choose the ILibraryPostScanTask fallback).
  </how-to-verify>
  <resume-signal>Type "approved — virtual seasons work" (with the recorded observations), or describe the failure signal observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Document the Wave 0 probe in the runbook</name>
  <read_first>
    - test/jellyfin-load-test/README.md (existing operator runbook structure)
    - .planning/phases/02-episodes-year-seasons/02-RESEARCH.md (verification commands + pass criteria table, lines ~999-1043)
  </read_first>
  <files>test/jellyfin-load-test/README.md</files>
  <action>
    Add a "## Phase 2 Wave 0 — Virtual Season Probe" section to the runbook capturing the exact operator command sequence and pass/fail criteria used in the Task 3 checkpoint: the config wipe + compose up, the /Library/Refresh + /Shows + /Shows/{id}/Seasons curl block (using the README's existing API_KEY pattern), the `[Youtarr]` log grep, the pass-criteria table (ChannelA Series, virtual Season 2024, ChannelB no-crash), and the failure-mode → response mapping (Season 1/Unknown → ParentIndexNumber discarded; no log lines → DI registration needed).
    Do not duplicate the whole Phase 1 runbook; append the new section after the existing live-verify content.
  </action>
  <verify>
    <automated>grep -q "Wave 0" test/jellyfin-load-test/README.md && grep -q "Seasons" test/jellyfin-load-test/README.md && grep -q "ParentIndexNumber" test/jellyfin-load-test/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README contains a Phase 2 Wave 0 section with the config-wipe + compose-up sequence.
    - The Seasons-listing curl command and pass-criteria table are documented.
    - The failure-mode → response mapping is documented for reproducibility.
  </acceptance_criteria>
  <done>The operator runbook documents how to re-run the virtual-season probe independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NFO file → plugin | Untrusted on-disk XML the plugin parses (stub does not read it; later plans do) |
| operator → Docker daemon | sudo required to start the daemon; agent never runs sudo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | media files mutated by Jellyfin metadata saver | mitigate | media mounted `:ro` in compose (Phase 1); README reiterates disabling "Save metadata to media folders" |
| T-02-02 | Denial of Service | malformed/empty NFO crashing scan | accept (probe) | Stub reads no NFO; full-parse hardening lands in 02-02; ChannelB empty-date fixture exercises the scan path live |
| T-02-03 | Elevation of Privilege | sudo for Docker daemon | transfer | Agent surfaces copy-paste sudo command; operator runs it (project policy) |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to gate |
</threat_model>

<verification>
- Plugin builds clean in Release with the stub provider.
- 6 episode/year fixtures present; existing fixtures intact.
- Live human-verify checkpoint records PASS/FAIL of virtual-season creation and DI auto-discovery.
</verification>

<success_criteria>
- A live Jellyfin 10.10.7 scan of the flat-layout ChannelA shows a virtual "Season 2024" container with both videos under it (PASS), OR a recorded FAIL signal that redirects 02-04 to the post-scan-task fallback.
- The probe result (and whether episode-provider DI auto-discovery worked) is recorded for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/02-episodes-year-seasons/02-01-SUMMARY.md` when done. Record the live probe outcome (PASS/FAIL), whether auto-discovery worked, and any failure signal observed — this gates 02-04.
</output>
