---
phase: 02-episodes-year-seasons
plan: 03
type: tdd
wave: 3
depends_on: ["02-02"]
files_modified:
  - Jellyfin.Plugin.Youtarr/Utils/PathUtils.cs
  - Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProvider.cs
  - Jellyfin.Plugin.Youtarr.Tests/Utils/PathUtilsTests.cs
  - Jellyfin.Plugin.Youtarr.Tests/Providers/YoutarrEpisodeNfoProviderTests.cs
requirements: [LIB-02, LIB-03, LIB-04, EPI-01, EPI-02, EPI-03, EPI-04, EPI-05, EPI-06, EPI-07, CMP-01, CMP-02]
must_haves:
  truths:
    - "A video with a co-located <movie> NFO becomes an Episode with title, plot (truncated, \\n→<br>), premiere date, runtime, genres, tags, YouTube id, and content rating populated"
    - "Episode.ParentIndexNumber = upload year when YearSeasons on; = 1 when off (LIB-04 flatten)"
    - "YYYYMMDD scheme sets IndexNumber = year*10000+month*100+day; Default scheme sets IndexNumber = null"
    - "A video with missing/invalid date is placed in Season 0 with IndexNumber null and does not crash"
    - "NFO sidecar is discovered via same-basename .nfo for both flat and nested layouts; missing NFO → HasMetadata false"
  artifacts:
    - path: "Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProvider.cs"
      provides: "ILocalMetadataProvider<Episode> mapping YoutarrVideoData → Episode"
      contains: "ILocalMetadataProvider<Episode>"
    - path: "Jellyfin.Plugin.Youtarr/Utils/PathUtils.cs"
      provides: "FindNfoForVideo sidecar resolver (flat + nested)"
      contains: "FindNfoForVideo"
    - path: "Jellyfin.Plugin.Youtarr.Tests/Providers/YoutarrEpisodeNfoProviderTests.cs"
      provides: "16+ mapping / fallback / numbering tests"
      contains: "GetMetadata_"
  key_links:
    - from: "YoutarrEpisodeNfoProvider.GetMetadata"
      to: "YoutarrNfoParser.Parse"
      via: "parse co-located NFO then map to Episode"
      pattern: "YoutarrNfoParser.Parse"
    - from: "YoutarrEpisodeNfoProvider"
      to: "Plugin.Instance.Configuration"
      via: "reads YearSeasons / EpisodeNumberingScheme / MaxDescriptionLength"
      pattern: "Plugin.Instance"
    - from: "YoutarrEpisodeNfoProvider"
      to: "Episode.ParentIndexNumber"
      via: "year-season assignment"
      pattern: "ParentIndexNumber"
---

<objective>
Implement the real `YoutarrEpisodeNfoProvider` — an `ILocalMetadataProvider<Episode>` that discovers the co-located NFO, parses it with `YoutarrNfoParser` (from 02-02), and maps the DTO onto a Jellyfin `Episode`: title, truncated plot, premiere date, runtime, genres, tags, YouTube provider id, content rating, year-based season assignment with the LIB-04 flatten toggle, the EPI-05 numbering schemes, and the EPI-07 date fallback to Season 0. Also add `PathUtils.FindNfoForVideo` for flat/nested sidecar discovery (CMP-01/CMP-02).

Purpose: This is the unit-tested core of Phase 2 — every requirement except the live virtual-season mechanism (validated in 02-01) is proven here against the field-mapping and fallback test tables, so the 02-04 live run only confirms end-to-end behavior rather than discovering logic bugs.

Output: `YoutarrEpisodeNfoProvider.cs`, `PathUtils.FindNfoForVideo`, and two test files (`YoutarrEpisodeNfoProviderTests.cs` 16+ cases, `PathUtilsTests.cs` additions), all green in Release.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-episodes-year-seasons/02-RESEARCH.md
@.planning/phases/02-episodes-year-seasons/02-02-SUMMARY.md
@Jellyfin.Plugin.Youtarr/Providers/YoutarrSeriesNfoProvider.cs
@Jellyfin.Plugin.Youtarr/Utils/PathUtils.cs
@Jellyfin.Plugin.Youtarr/Plugin.cs
@Jellyfin.Plugin.Youtarr/PluginServiceRegistrator.cs
@Jellyfin.Plugin.Youtarr.Tests/Providers/YoutarrSeriesNfoProviderTests.cs

<interfaces>
<!-- Consumed from 02-02 (already implemented) and Jellyfin. -->

From 02-02 — YoutarrNfoParser.Parse(string) → YoutarrVideoData? (throws XmlException on malformed XML).
YoutarrVideoData fields: Title, Plot, PremiereDate:DateTime?, DateAdded:DateTime?, RuntimeMinutes:int?,
  DurationInSeconds:int?, Studio, YouTubeId, MpaaRating, Genres:List<string>, Tags:List<string>.

From 02-02 — PluginConfiguration: YearSeasons:bool, EpisodeNumberingScheme (Default|YYYYMMDD), MaxDescriptionLength:int.
Config access pattern (from Plugin.cs): `Plugin.Instance?.Configuration ?? new PluginConfiguration()`.

Jellyfin Episode (MediaBrowser.Controller.Entities.TV.Episode) properties used:
  Name:string, Overview:string?, PremiereDate:DateTime?, ProductionYear:int?,
  ParentIndexNumber:int?, IndexNumber:int?, RunTimeTicks:long?,
  Genres:string[], Tags:string[], Studios:string[], OfficialRating:string?,
  ProviderIds (IDictionary — set ProviderIds[Constants.YouTubeProviderId] = id).

Provider shape (mirror YoutarrSeriesNfoProvider exactly, Series→Episode):
  ILocalMetadataProvider<Episode>, IHasItemChangeMonitor;
  Name => Constants.ProviderName;
  Task<MetadataResult<Episode>> GetMetadata(ItemInfo info, IDirectoryService, CancellationToken);
  result.HasMetadata = true REQUIRED on success (Pitfall 1).

RunTimeTicks: TimeSpan.FromSeconds(durationInSeconds).Ticks (prefer) else TimeSpan.FromMinutes(runtimeMinutes).Ticks (Pitfall 4 — never manual multiply).
Description format (EPI-04): truncate to MaxDescriptionLength via range operator, then Replace("\n","<br>", StringComparison.Ordinal).
YYYYMMDD (EPI-05): (year*10000)+(month*100)+day. Default scheme: IndexNumber = null.
EPI-07: no valid date → ParentIndexNumber = 0 (Season 0), IndexNumber = null, PremiereDate = null.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PathUtils.FindNfoForVideo sidecar resolver (RED → GREEN)</name>
  <read_first>
    - Jellyfin.Plugin.Youtarr/Utils/PathUtils.cs (existing helper style, null-tolerance, no MediaBrowser types)
    - Jellyfin.Plugin.Youtarr.Tests/Utils/PathUtilsTests.cs (temp-file test pattern + where to append)
    - .planning/phases/02-episodes-year-seasons/02-RESEARCH.md (FindNfoForVideo skeleton lines ~823-840; CMP-01/CMP-02 lines ~802-821; PathUtils test additions lines ~1092-1099)
  </read_first>
  <behavior>
    - FindNfoForVideo_NfoExists_ReturnsSiblingPath: video.mp4 with video.nfo present → returns the .nfo path.
    - FindNfoForVideo_NfoMissing_ReturnsNull: video.mp4 without sidecar → null.
    - FindNfoForVideo_NestedLayout_ResolvesCorrectly: Channel/Title/Title.mp4 with Channel/Title/Title.nfo → correct sidecar path.
    - FindNfoForVideo_NullInput_ReturnsNull: null/empty/whitespace → null.
  </behavior>
  <files>Jellyfin.Plugin.Youtarr/Utils/PathUtils.cs, Jellyfin.Plugin.Youtarr.Tests/Utils/PathUtilsTests.cs</files>
  <action>
    RED first: append the four FindNfoForVideo tests to PathUtilsTests.cs (create temp dirs/files for the flat + nested cases). Confirm they fail to compile (method missing) before implementing.
    Then add `public static string? FindNfoForVideo(string? videoPath)` to PathUtils: return null for null/empty/whitespace; compute `Path.ChangeExtension(videoPath, ".nfo")`; return it only if `File.Exists`, else null. Pure — no side effects beyond the existence check. Same-basename co-location handles both flat and nested layouts identically (CMP-01/CMP-02).
  </action>
  <verify>
    <automated>dotnet test Jellyfin.Plugin.Youtarr.Tests/Jellyfin.Plugin.Youtarr.Tests.csproj -c Release --filter "FullyQualifiedName~PathUtilsTests"</automated>
  </verify>
  <acceptance_criteria>
    - FindNfoForVideo returns the sibling .nfo path when present (flat and nested), null otherwise and for null/empty input.
    - All PathUtilsTests (existing 12 + 4 new) pass in Release.
    - RED→GREEN observed (failing test commit precedes implementation commit).
  </acceptance_criteria>
  <done>NFO sidecar discovery works identically for flat and nested Youtarr layouts (CMP-01/CMP-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: YoutarrEpisodeNfoProvider — NFO→Episode mapping (RED → GREEN)</name>
  <read_first>
    - Jellyfin.Plugin.Youtarr/Providers/YoutarrSeriesNfoProvider.cs (provider structure, HasMetadata, try/catch around NFO read, IHasItemChangeMonitor)
    - Jellyfin.Plugin.Youtarr.Tests/Providers/YoutarrSeriesNfoProviderTests.cs (ItemInfo construction via `new ItemInfo(new Episode()) { Path = ... }`, Moq ILogger/IDirectoryService)
    - .planning/phases/02-episodes-year-seasons/02-RESEARCH.md (MapToEpisode + FormatDescription skeleton lines ~329-419; field map lines ~711-740; EPI-07 fallback lines ~744-769; provider test table lines ~1071-1090; Pitfalls 1,4,6)
  </read_first>
  <behavior>
    - GetMetadata_NoNfoFile_HasMetadataFalse: video with no .nfo → HasMetadata == false.
    - GetMetadata_ValidNfo_HasMetadataTrue and GetMetadata_Title_MappedToName.
    - GetMetadata_Plot_TruncatedToMaxLength (Overview length == MaxDescriptionLength) and GetMetadata_Plot_NewlinesConverted (contains <br>).
    - GetMetadata_ValidYear_ParentIndexNumberIsYear (2024-03-15 → 2024).
    - GetMetadata_YearSeasonsOff_ParentIndexNumber1 (LIB-04, config.YearSeasons=false → 1).
    - GetMetadata_MissingDate_ParentIndexNumber0 and GetMetadata_MissingDate_IndexNumberNull (EPI-07 → Season 0).
    - GetMetadata_YYYYMMDD_CorrectIndexNumber (→ 20240315) and GetMetadata_Default_IndexNumberNull.
    - GetMetadata_DurationSeconds_RunTimeTicks (300s → 3_000_000_000) and GetMetadata_RuntimeMinutes_RunTimeTicks (5min, no duration → 3_000_000_000).
    - GetMetadata_YouTubeId_InProviderIds (ProviderIds["YouTube"] == id), GetMetadata_Mpaa_OfficialRating ("PG-13"), GetMetadata_NoMpaa_OfficialRatingNull.
    - Genres and Tags arrays populated from multi-element NFO.
  </behavior>
  <files>Jellyfin.Plugin.Youtarr/Providers/YoutarrEpisodeNfoProvider.cs, Jellyfin.Plugin.Youtarr.Tests/Providers/YoutarrEpisodeNfoProviderTests.cs</files>
  <action>
    RED first: write YoutarrEpisodeNfoProviderTests.cs covering every case in the behavior block. Construct ItemInfo as `new ItemInfo(new Episode()) { Path = videoPath }`; for config-dependent tests (YearSeasons off, YYYYMMDD), set Plugin.Instance configuration — if Plugin.Instance is not constructible in a unit test, extract MapToEpisode/FormatDescription as internal static methods taking an explicit PluginConfiguration and test those directly (keep GetMetadata thin). Confirm RED (types/methods missing) before implementing.
    Then implement YoutarrEpisodeNfoProvider mirroring YoutarrSeriesNfoProvider's structure:
      - Name => Constants.ProviderName.
      - GetMetadata: nfoPath = Path.ChangeExtension(info.Path, ".nfo"); if !File.Exists → return result (HasMetadata false). Parse inside try/catch (XmlException/Exception → log warning, return HasMetadata false — Pitfall 1/T-02-04). If Parse returns null → HasMetadata false.
      - Read config via `Plugin.Instance?.Configuration ?? new PluginConfiguration()`.
      - Map via MapToEpisode(data, config):
          Name = Title; Overview = FormatDescription(Plot, MaxDescriptionLength) [truncate via range op + "\n"→"<br>" ordinal — EPI-04].
          If PremiereDate valid: set PremiereDate, ProductionYear=year; ParentIndexNumber = YearSeasons ? year : 1 (LIB-03/LIB-04); IndexNumber = YYYYMMDD ? (y*10000+m*100+d) : null (EPI-05).
          Else (EPI-07): ParentIndexNumber = 0, IndexNumber = null, PremiereDate = null.
          RunTimeTicks: prefer TimeSpan.FromSeconds(DurationInSeconds).Ticks else TimeSpan.FromMinutes(RuntimeMinutes).Ticks (Pitfall 4).
          Genres/Tags → arrays when non-empty; YouTubeId → ProviderIds[Constants.YouTubeProviderId]; MpaaRating → OfficialRating (EPI-06); Studio → Studios[].
      - result.Item = episode; result.HasMetadata = true.
      - IHasItemChangeMonitor.HasChanged: return File.GetLastWriteTimeUtc(nfoPath) > item.DateLastSaved when the sidecar exists, else false (re-read on NFO update).
    Reuse `Path.ChangeExtension` directly in GetMetadata (PathUtils.FindNfoForVideo from Task 1 may also be used; either is acceptable since both resolve identically).
    Do NOT add explicit DI registration here yet — auto-discovery is the default (Pitfall 7); the 02-01 probe records whether explicit registration is needed, and 02-04 adds it only if so.
    Run the provider test class green in Release; then run the FULL suite to confirm no Phase 1 regression.
  </action>
  <verify>
    <automated>dotnet build Jellyfin.Plugin.Youtarr/Jellyfin.Plugin.Youtarr.csproj -c Release && dotnet test Jellyfin.Plugin.Youtarr.Tests/Jellyfin.Plugin.Youtarr.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior-block case passes (16+ provider tests) and the FULL suite (Phase 1 + 02-02 + this plan) is green in Release.
    - YearSeasons toggle maps to ParentIndexNumber year vs 1; YYYYMMDD vs Default maps IndexNumber correctly; missing date → Season 0 + null IndexNumber.
    - RunTimeTicks uses TimeSpan (no manual ticks multiply); plot truncated to MaxDescriptionLength with \n→<br>.
    - Missing NFO and malformed NFO both yield HasMetadata == false without throwing.
    - Plugin still publishes clean (no MediaBrowser.* DLLs) — confirm via `dotnet publish -c Release`.
    - RED→GREEN observed.
  </acceptance_criteria>
  <done>The full NFO→Episode mapping is implemented and unit-proven for every Phase 2 metadata, season, numbering, and fallback requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NFO file → provider | Untrusted on-disk XML parsed per video; result becomes an Episode item |
| config → provider | User-set MaxDescriptionLength / scheme / YearSeasons read at scan time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Denial of Service | malformed NFO crashing GetMetadata | mitigate | try/catch around Parse → log + HasMetadata false; covered by missing/malformed NFO tests |
| T-02-09 | Denial of Service | unbounded plot stored on Episode | mitigate | FormatDescription truncates to MaxDescriptionLength before assignment (EPI-04); test asserts length |
| T-02-10 | Tampering | wrong year from download date | mitigate | ParentIndexNumber/PremiereDate use `<premiered>` only, never `<dateadded>` (Pitfall 6); date validated in 02-02 parser |
| T-02-11 | Denial of Service | RunTimeTicks integer overflow on long videos | mitigate | TimeSpan.FromSeconds(...).Ticks (long arithmetic) instead of manual multiply (Pitfall 4) |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to gate |
</threat_model>

<verification>
- Plugin builds clean in Release; full xUnit suite green (Phase 1 24 + 02-02 parser + PathUtils additions + provider tests).
- Publish output free of MediaBrowser.* DLLs (ExcludeAssets invariant preserved).
- Every requirement LIB-02/03/04, EPI-01..07, CMP-01/02 has a green unit test.
</verification>

<success_criteria>
- A video with a `<movie>` NFO maps to a fully-populated Episode (title, plot, date, runtime, genres, tags, YouTube id, rating).
- Year-season assignment and the LIB-04 flatten toggle, both EPI-05 numbering schemes, and the EPI-07 Season-0 fallback are all correct and unit-tested.
- NFO discovery works for flat and nested layouts; missing/malformed NFOs degrade gracefully to HasMetadata false.
</success_criteria>

<output>
Create `.planning/phases/02-episodes-year-seasons/02-03-SUMMARY.md` when done.
</output>
